<% let baseColor = 'bg-blue-500 text-white hover:bg-blue-300'; 
if (variant === 'delete') { 
    baseColor = 'bg-red-700 text-white hover bg-red-500'; 
  } 
%>

<button
  type="<%= type || 'button' %>"
  class="py-2 px-3 text-sm border rounded-lg hover:cursor-pointer <%= baseColor %>"
>
  <%= text %>
</button>
